services:
  api:
    profiles:
      - future-service-mode
    image: python:3.12-slim
    command:
      - python
      - -c
      - "raise SystemExit('CGG API source contract exists, but Docker Compose runtime launch is denied until the dev-integration profile is active.')"
