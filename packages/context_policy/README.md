# Context Policy Package

Placeholder package for later policy integration.

Phase 1 keeps policy deterministic and local. OPA/Rego integration belongs in
the full-maturity follow-up after the runtime lane is admitted.
