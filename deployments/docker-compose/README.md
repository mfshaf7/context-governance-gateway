# Docker Compose Deployment

No active Docker Compose deployment exists yet.

The API/source contract is present under `apps/api`, but runtime launch remains
denied until the `context-governance-gateway` `dev-integration` profile becomes
`active`. Docker Compose assets must not be treated as stage or prod evidence.
