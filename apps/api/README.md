# API App

The API app is intentionally not implemented in Phase 1.

Service mode requires a registered `dev-integration` profile and security,
platform, and workspace-governance admission before this repo adds a runnable
API, worker, database-backed storage, dashboard, broker adapter, or cross-repo
runtime behavior.
